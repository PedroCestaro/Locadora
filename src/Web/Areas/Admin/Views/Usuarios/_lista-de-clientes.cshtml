@model TClient
<div class="col-lg-12 item-cliente"> 
    <input type="hidden"  name="Clientes" class="id-cliente" value="@Model.Id" /> 
        <p>
        <strong>@Model.Name</strong> - <a class="excluir-cliente" data-nome="@Model.Name" style="cursor: pointer">Excluir</a>
    </p>
</div>

