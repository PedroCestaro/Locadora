@model List<TReservation>
@{
    ViewBag.Title = "ReservasDeClientes";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var cliente = ViewBag.Cliente;
}

<h2>Reservas De Clientes</h2>

<div class="row">
    <h3>Buscar Por Cliente</h3>
    <div class="col-lg-12">
        @this.Label("Cliente").Class("Cliente").Value("Cliente:")
        <input type="text" class="loginCliente" />
        <button class="busca" type="submit">Buscar</button>
    </div>
    <h3>Buscar Por Filmes</h3>
    <div class="col-lg-12">
        @this.Label("Filmes").Class("Filmes").Value("Filmes:")
        @this.AutoSelect("Filmes").Class("Filmes").FirstOption("", "Selecione uma opção")
    </div>
    <div>
        <label>Status da Reserva</label>
        <select class="status">
            <option value="">Selecione uma opção</option>
            <option value="false">Ativa</option>
            <option value="true">Finalizada</option>
        </select>
    </div>
    @*<div class="col-lg-12>
        <select class="status" name="Returned" id="status">
            <option value="0">Ativa</option>
            <option value="1">Finalizada</option>
         </select>   
    </div>*@

    <div class="lista-de-reservas">
    @Html.Partial("_lista_de_reservas", Model)
    </div>

    </div>

<script type="text/javascript">

    $(function () {

        $("select.status").change(function () {
            $(".status").val();
        });

     $("button.busca").click(function () {
         var login = $("input.loginCliente").val();
         var movieId = $("Select.Filmes").val();
         var returned = $("select.status").val();
         $.post("@Url.Action(MVC.Admin.Usuarios.ListarReservas())", { login: login, movieId: movieId, returned:returned }, function (data) {//variavel do backend, variavel que eu passo pelo front
             $("div.lista-de-reservas").html(data);
          });
     });


  });


   @*$("#tipoData").change(function () {
            if ($(this).val() == 3) {
                $("#Status").val("@OrderStatus.Canceled");
                $("#Status").prop("disabled", "disabled");
            }
            else if ($(this).val() == 2) {
                $("#Status").val("@OrderStatus.Paid");
                $("#Status").prop("disabled", "disabled");
            }
            else
                $("#Status").prop("disabled", "");
            Buscar(1);
        });*@
</script>
